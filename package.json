{
  "name": "stillwater-airfield",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "npm run test:surface && npm run test:simulation && npm run test:collisions",
    "test:surface": "node scripts/validateSurfaceGraphs.mjs",
    "test:simulation": "node scripts/validateFixedStep.mjs",
    "test:collisions": "node scripts/validateCollisionEnvelopes.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.7"
  }
}
